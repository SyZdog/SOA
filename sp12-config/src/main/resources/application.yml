spring:
  application:
   name: config-server
  cloud:
    config:
      server:
        git:  
          uri: https://github.com/SyZdog/SOA
          searchPaths: Config
#          username: 私有仓库需添加
#          password: 私有仓库需添加
server:
  port: 6001

eureka:
  client:
    service-url:
      defaultZone: http://eureka1:2001/eureka, http://eureka2:2002/eureka
      
