spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: 
server:
  port: 6001
  
eureka:
  client: 
    service-url:
      defaultZone: http://eureka1:2011
  